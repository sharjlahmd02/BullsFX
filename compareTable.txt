import { lowRiskPlans } from "../pages/data/LowRiskPlans";
import { Check } from "lucide-react";

const ComparisonGrid = ({ isDark }: { isDark: boolean }) => {
  return (
    <section className="mt-24">
      {/* Heading */}
      <div className="mb-14 text-center">
        <p className="uppercase tracking-widest text-xs opacity-60 mb-3">
          Compare Investment Tiers
        </p>
        <h2 className="text-3xl md:text-4xl font-semibold">
          Choose the Plan That Fits You
        </h2>
      </div>

      {/* Grid */}
      <div className="grid gap-8 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5">
        {lowRiskPlans.map((plan) => (
          <div
            key={plan.id}
            className={`group relative rounded-3xl p-6 transition-all duration-300
              ${
                isDark
                  ? "bg-white/5 border border-white/10 hover:bg-white/10"
                  : "bg-white border border-neutral-200 hover:shadow-xl"
              }
            `}
          >
            {/* Glow effect */}
            <div className="absolute inset-0 rounded-3xl opacity-0 group-hover:opacity-100 transition pointer-events-none
              bg-[radial-gradient(circle_at_top,#08CB0030,transparent_60%)]" />

            {/* Tier */}
            <h3 className="text-xl font-semibold text-[#08CB00] mb-6">
              {plan.level}
            </h3>

            {/* Info */}
            <Info label="Investment Range" value={plan.investment} />
            <Info label="Monthly ROI" value={plan.profit} highlight />
            <Info label="Duration" value={plan.duration} />
            <Info label="Distribution" value={plan.distribution} />

            {/* CTA */}
            <button
              className={`mt-6 w-full py-3 rounded-xl text-sm font-medium transition
                ${
                  isDark
                    ? "bg-white/10 hover:bg-white/20"
                    : "bg-neutral-100 hover:bg-neutral-200"
                }
              `}
            >
              View Details
            </button>
          </div>
        ))}
      </div>
    </section>
  );
};

export default ComparisonGrid;

/* ---------- Small Helper ---------- */

const Info = ({
  label,
  value,
  highlight,
}: {
  label: string;
  value: string;
  highlight?: boolean;
}) => (
  <div className="mb-4">
    <p className="text-xs opacity-60 mb-1">{label}</p>
    <p
      className={`text-sm font-medium ${
        highlight ? "text-[#08CB00]" : ""
      }`}
    >
      {value}
    </p>
  </div>
);
